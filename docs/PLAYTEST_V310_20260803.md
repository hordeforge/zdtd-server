# zdtd stock-client demo on V3.1.0 (2026-08-03)

## Version pin (join blocker)

Client rejected with `Game Version Mismatch: V 3.1.0 vs V 3.0.1`.

| Surface | Value |
|---|---|
| PackageIds VersionInfo | major=3 minor=**10** build=**14** |
| GSI ServerVersion | `V 3.1.0` |
| Unit tests | **239/239** (+ PDF bag pad / SetBlock damage / materials) |

## Admin / fixture parity (kill barrier blocker)

Playtest orch fixtures were **stock-only** (`want_fixtures = server=="stock"`).
Enabled for **zdtd** and taught admin TCP stock-ish commands:

- `listplayers` / `lp` with `id=N` lines
- `settime 8000` / `22000` tick form
- `killall`
- `spawnentity <playerEntityId|slot> <class>` (vehicles/traders/zombies)
- orch parsers accept `entity=` and `kind=zombie`

## Demo progression

| Run | pass | fail | Notes |
|---|---:|---:|---|
| first (version only) | 73 | 10 | join ok; kill barriers never ran |
| fixtures stock-only | 72 | 11 | still no telnet on zdtd |
| fixtures + admin parity | **76** | **7** | kill/spawn/listplayers work |
| + respawn join bundle | **75** | **8** | **player_respawn PASS**; dig pad flaky |
| weather defer + spawnSurface (20260804c) | **77** | **6** | dig/place/loot/craft PASS; join healthy |
| residual fix pack (g/i) | **77-78** | **5-6** | dig/place/melee/loot pickup PASS |
| PDF bLoaded + ECD v36 player (k) | **78** | **5** | eat_food PASS; wood/coins on client |

Latest log: `server/logs/playtest_zdtd_demo_20260803k.log`

### Now PASS that were FAIL

- `combat/zombie_death_loot`
- `economy/zombie_removed_after_kill`
- `finale/player_respawn` (hp=100, alive=True)
- `core/dig_confirm` / `core/place_confirm` (authoritative SetBlock S2C)
- `core/block_damage_melee` (absolute damage echo; materials MaxDamage)
- `economy/loot_bag_pickup` (EntityCollect + EntityRemove Despawned)
- `economy/eat_food_consume` (PDF `bLoaded=true` + player ECD → ToPlayer applies bag)

### Residual FAIL (product depth, not join)

1. `combat/explosion_client` - melee soft-block without close-in step (raycast miss)
2. `economy/bag_add_item` - bag 45/45 full (starter + junk occupancy; AddItem false)
3. `economy/craft_consume_output` - wood0=35 present but craft queue does not consume
4. `economy/trader_buy` - coins0=10250 present; local buy path does not spend
5. intermittent: `world/block_sample_ring` solid=0

**PDF bag root cause (fixed):** `bLoaded` was false → client skipped `ToPlayer`.
Also ECD FileVersion **36** + `stressAmount`, playerMale+profile for CreateEntity.

## Server fixes shipped this pass

- SetBlock: apply then S2C result body with absolute `BlockValue.damage`
- materials.xml MaxDamage → hayBaleSquare etc.
- ItemDrop → class `item` ECD; Collect → Remove reason Despawned
- ECD v36 + stressAmount; PDF bLoaded=true + player profile ECD
- PDF bag pad = CarryCapacity 45; starter casinoCoin; orch zdtd `--fresh-save`

## Honest status

- **Join / dig / place / melee dmg / loot / eat:** PASS
- **Full demo green:** not yet; residual = bag slot waste, craft queue, trader spend, explosion aim

Unit tests **239/239** (ReleaseFast binary rebuild). Latest log: `server/logs/playtest_zdtd_demo_20260804c.log`.

## Follow-up fix (spawn surface)

`Game.spawnSurface` snaps join/respawn to DTM height and places `terrDirt` if the surface cell is air, so `BlockUnderFeet` dig/place samples see solid terrain. Admin `version` now reports `V3.1.0 wire`. Unit tests: **239/239**. Re-run `make playtest-zdtd` to re-score dig/place residual after this fix.
