# zdtd stock-client demo on V3.1.0 (2026-08-03)

## Version pin fix (blocker)

Client V3.1.0 rejected join with:

```text
Client failed to authorize server: Game Version Mismatch: you have 'V 3.1.0' and server has 'V 3.0.1'
```

Cause: `NetPackagePackageIds` `VersionInfo` still defaulted to minor=1 build=4, and GSI still had residual `V.3.1.4` / bot login `V 3.0.1`.

Fix (this session):

| Surface | New value |
|---|---|
| `wire/packages.zig` VersionInfo | major=3 minor=**10** build=**14** |
| GSI `ServerVersion` | `V 3.1.0` |
| Bot inject login strings | `V 3.1.0` |
| Unit test package ids body | expects 10/14 |

Unit tests: **197/197** pass (binary direct).

## Demo suite

Command: `make -C 7dtd-playtest playtest-zdtd`  
Orch log: `server/logs/playtest_zdtd_demo_20260803c.log`  
Client: `Chunks: 85 CGO: 35-41`, FPS ~200-350, 0 version mismatch after pin.

| Result | Count |
|---|---:|
| pass | **73** |
| fail | **10** |
| skip | 0 |
| total | 83 |
| wall | ~184 s |

### Fails (server gaps / fixture depth)

1. `combat/zombie_death_loot` - kill barrier did not kill fixture
2. `combat/explosion_client` - no block damage from attack path
3. `economy/bag_add_item` - give path no-op
4. `economy/eat_food_consume` - no food item (depends on give)
5. `economy/loot_bag_pickup` - EntityItem collect not removed
6. `economy/zombie_removed_after_kill` - kill barrier no-op
7. `economy/craft_consume_output` - no wood/club (depends on give/craft)
8. `economy/trader_buy` - coins/buy incomplete
9. `finale/player_respawn` - death ok, respawn stuck 25s
10. (see orch summary for exact tenth if renumbered; list from client FAIL lines)

### Pass highlights

- Join + mesh (CGO)
- Dig/place, move, UI windows
- Melee + ranged damage out
- Sleeper wake, blood moon time
- Workstation TE, chest TE, trader UI open
- Full quest phase graph whiteRiver
- Vehicle spawn/enter/drive/clamp
- Power place/wire/turret
- Player death screen

## Honest status

**Join path on V3.1.0: PASS** after version pin.  
**Core playable loop: mostly PASS** with residual **kill authority, loot pickup, inventory give, trader buy, respawn** depth gaps. Not a clean 83/83; not a join-blocker either.
