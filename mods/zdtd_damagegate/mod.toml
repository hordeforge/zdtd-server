name = "zdtd_damagegate"
version = "0.1.0"
wasm = "zdtd_damagegate.wasm"
description = "Incoming-player damage scaling via the on_player_damage verdict (0.5x default; specialize per attacker). Enable via zdtd.toml [plugin] modules = \"mods/zdtd_damagegate/zdtd_damagegate.wasm\"."
