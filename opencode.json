{
  "$schema": "https://opencode.ai/config.json",
  "model": "grok-cli/grok-4.5",
  "small_model": "grok-cli/grok-4.5",
  "compaction": {
    "auto": true,
    "prune": true,
    "reserved": 20000
  },
  "formatter": {
    "zig": {}
  },
  "lsp": {
    "zls": {
      "command": ["zls"],
      "extensions": [".zig", ".zon"]
    }
  },
  "command": {
    "check": {
      "description": "Build and run the full zig test suite (make check)",
      "template": "Run the project validation loop from the repo root:\n\n```bash\nmake check\n```\n\nThat is `zig build` then `zig build test`. Fix any compile or test failures. Do not skip failing tests. Prefer the smallest correct fix over drive-by refactors."
    },
    "test": {
      "description": "Run zig build test only",
      "template": "From the repo root run:\n\n```bash\nzig build test\n```\n\nReport failures with file:line and fix them. Keep production wire fidelity (no invented package shapes, no hard-coded package ids as permanent)."
    },
    "lint": {
      "description": "Run wire/style lint (scripts/lint-wire.sh)",
      "template": "From the repo root run:\n\n```bash\nmake lint\n```\n\nThis is a ripgrep-based lint (ast-grep has no Zig AST). Fix real findings. Test-only fixtures may use fixed package ids; production send/receive paths must resolve ids via the negotiated name map."
    },
    "run": {
      "description": "Build and run zdtd with default flat world",
      "template": "Build and start the server for local smoke testing:\n\n```bash\nzig build\nzig-out/bin/zdtd --port 27002 --world worlds/zdtd_default\n```\n\nIf the user asked for a stock map, use `--game-dir` and `--world-name` / `--map` per AGENTS.md. Do not leave a long-running process hanging without saying how to stop it."
    },
    "apm": {
      "description": "How to read zdtd native APM dumps",
      "template": "Use **zdtd native APM** only (`src/apm/`, docs/APM.md). Do not involve 7dtd-apm.\n\n1. Read `docs/APM.md` for counters and sections.\n2. Run the server under the scenario of interest, then inspect exit dumps or configured JSON/text snapshots.\n3. Summarize hot sections (net_poll, sim_entities, replicate, net_flush) with concrete numbers.\n4. If instrumentation is missing on a hot path you touched, add `apm` section/counter hooks as part of the fix."
    }
  },
  "provider": {
    "grok-cli": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "Grok (CLI auth)",
      "options": {
        "baseURL": "https://api.x.ai/v1",
        "apiKey": "{env:XAI_API_KEY}"
      },
      "models": {
        "grok-4.5": {
          "name": "Grok 4.5",
          "limit": {
            "context": 400000,
            "output": 16384
          }
        },
        "grok-build": {
          "name": "Grok Build",
          "limit": {
            "context": 400000,
            "output": 16384
          }
        }
      }
    }
  }
}
