---
description: Run zig build test only
agent: build
---

From the repo root run:

```bash
zig build test
```

Report failures with file:line and fix them. Keep production wire fidelity (no invented package shapes, no hard-coded package ids as permanent).
