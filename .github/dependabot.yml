version: 2
updates:
  # Product has no npm/cargo/zig registry deps; only pin Actions SHAs.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
